namespace: grocery
resources:
    - ../../base
commonLabels:
    app: grocery-be
configMapGenerator:
    - name: grocery-be-properties
      literals:
          - spring.datasource.url=jdbc:postgresql://192.168.86.5:5432/grocery
          - spring.datasource.username=grocerybe
          - grocery-be.external.food-details-integrator-be.base-url=http://food-details-integrator-be-service.grocery.svc.cluster.local:8080
secretGenerator:
    - name: grocery-be-secrets
      literals:
          - spring.datasource.password=Password123
patchesStrategicMerge:
    - deployment_patch.yaml